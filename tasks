#include <stdio.h>

void assignValueMinusOne(int *numPtr) {
    *numPtr = -1;
}

int main() {
    int num = 0;

    printf("%d\n", num);

    assignValueMinusOne(&num);

    printf("%d\n", num);

    return 0;
}
