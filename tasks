#include <stdio.h>

int main() {
    unsigned short int value = 0xABCD;

    unsigned char msb = (value >> 8) & 0xFF;
    unsigned char lsb = value & 0xFF;

    printf("Most Significant Byte: 0x%02X\n", msb);
    printf("Least Significant Byte: 0x%02X\n", lsb);

    return 0;
}
